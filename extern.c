#include<stdio.h>
int a=5;
int main() {
printf("a=%d\n",a);                             /*Extern is a keyword in C programming language 
                                                    which is used to declare a global variable
                                      that is a variable without any memory assigned to it*/
int a=1000;
printf("a=%d\n",a);
return 0;
}
